# Code Snippets

## MCP Usage

This project has the `codebase-memory-mcp` MCP server configured. **Always use it for code exploration and context gathering before reading files directly.**

- Use `codebase-memory-mcp` tools to search for symbols, understand structure, and gather context.
- Only fall back to direct file reads when the MCP server cannot provide the needed information.
